/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img.freepik.com',  // Add this domain
            'th.bing.com',      // And any other domains you use
            // Add more domains as needed
          ]
    },
  };

export default nextConfig;
